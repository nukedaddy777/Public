<Window x:Class="WpfMenuTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMenuTestApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem StaysOpenOnClick="True" Header="Materials">
                <ListBox x:Name="LbMaterialx" Margin="4,4,4,4" 
                         VerticalAlignment="Stretch" SelectionMode="Single" FontSize="12">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" BorderBrush="Black">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="5*"/>
                                        <RowDefinition Height="1*"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Background="AliceBlue" Click="Select_Click">
                                        Select Material
                                    </Button>
                                    <TextBox Grid.Column="1" Text="{Binding MaterialName}" Background="Beige"/>
                                    <Button Grid.Column="2" Background="{Binding MaterialColor}" Click="ColorSelector_Click">
                                        Select Color
                                    </Button>
                                    <Separator Grid.Row="1"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <MenuItem Header="Add Material" Click="AddMaterial_Click"/>
            </MenuItem>
            <MenuItem Header="Test" Click="Test_Click"/>
        </Menu>
        <TextBox x:Name="txtBoxConsole" Grid.Row="1" IsReadOnly="True" />
    </Grid>
</Window>
